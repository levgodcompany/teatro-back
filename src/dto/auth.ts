export interface Auth {
    email: string;
    password: string;
}

export interface RegisterClientAuthDto {
    name: string;
    lastName: string;
    email: string;
    cel: string;
    password: string;
}